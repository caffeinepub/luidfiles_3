{
  "frontend/src/pages/SharePage.tsx": "Share page updated to use the `useStreamingDownload` hook with a visible Progress bar during download, showing percentage in the button label; all user-facing text is in Portuguese.",
  "frontend/src/components/FileList.tsx": "File list component now uses the new `useStreamingDownload` hook for downloads, shows a per-file inline progress bar during download, and has Portuguese UI text; the old `useDownloadFile` mutation is replaced.",
  "frontend/src/hooks/useQueries.ts": "Updated React Query hooks with staleTime of 60s on file list, session, and user queries for stale-while-revalidate behavior; logout now clears both chunk cache and sessionStorage metadata; delete mutation also evicts the chunk cache.",
  "frontend/src/pages/AdminPage.tsx": "Admin panel with full Master capabilities: role-change dropdown (Staff/Client) for non-Master users visible only to Master, delete button for Staff accounts (Master only) and Client accounts (Master+Staff), and accurate storage meter in ClientProfile using real `getUserStorageStats` data.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard now rehydrates file metadata from sessionStorage into React Query cache on mount for instant rendering, persists fresh file metadata to sessionStorage after each fetch, and shows an \"ICP Optimized\" badge in the header.",
  "frontend/src/App.tsx": "Main app router with all routes; wraps DashboardPage and AdminPage in thin wrapper components (DashboardWrapper, AdminWrapper) that read session token and role from hooks/storage so the router can mount them without props.",
  "frontend/src/hooks/useChunkedUpload.ts": "Refactored chunked upload hook that pipelines uploads in concurrent batches of 3 chunks, tracks per-chunk progress percentage, calculates estimated time remaining (ETA) in real time, and exposes `estimatedSecondsRemaining` in the progress state.",
  "frontend/src/components/ChunkedUpload.tsx": "Upload UI updated to display per-chunk progress using the shadcn Progress bar, show estimated time remaining with a Clock icon, and include a visible \"Iniciar Upload\" button; all user-facing text is in Portuguese.",
  "frontend/src/utils/chunkCache.ts": "New utility module providing a module-level in-memory Map for caching downloaded file chunk blobs (keyed by fileId+chunkIndex), plus sessionStorage helpers to persist and rehydrate file metadata (filename, size, MIME type, totalChunks) across page loads.",
  "frontend/src/hooks/useStreamingDownload.ts": "New hook that wraps the backend `downloadFile` call with simulated per-chunk progress feedback, caches the assembled Blob in the chunkCache for instant repeat downloads, and triggers a browser file download with the correct filename and MIME type."
}